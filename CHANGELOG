# Changelog to 2.x version:

- all actions is in $acl property
- use classes for install plugin
- classes for create rules
- AND, OR coditions to compile rules
- create local rules
- check with not option

# Changelog to 3.x version:

- replace `$access` method to `access` data.
- best perfomance.
- global fail route.
- reactivity include to best usage.
- prevent refresh page, current permission is save ans get from sessionStorage automatic.